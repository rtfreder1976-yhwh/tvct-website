---
// Retired 2026-08-29. This page hosted a BookingKoala office-booking iframe.
// Commercial capture is now site-owned: /request-a-quote -> /quote-submit ->
// n8n -> GoHighLevel, so no customer-facing route may embed or link the
// BookingKoala booking form (CLAUDE.md §2).
//
// The route was already retired in practice — excluded from the sitemap, no
// inbound links, and shadowed by the vercel.json redirect that fires at the
// edge before Astro renders. This redirect is the in-app backstop for that.
export const prerender = false;

return Astro.redirect("/request-a-quote?service=commercial", 301);
---
