---
export const prerender = false;

// The legacy /dashboard implementation duplicated /admin/dashboard and carried
// hand-entered sample metrics. Keep one authenticated dashboard surface and
// send old bookmarks to it without forwarding query-string credentials.
return Astro.redirect('/admin/dashboard', 308);
---
